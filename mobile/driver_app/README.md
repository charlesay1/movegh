# MoveGH Driver App (Scaffold)

This is the driver-facing Flutter app scaffold for MoveGH.

## Run (from this folder)
```bash
flutter pub get
flutter run
```

## Flow
Welcome → Phone → OTP → Profile → Location → Home

## Notes
- Auth and dispatch are mocked for now.
- Uses the same theme/colors as the Rider app.
